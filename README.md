# how-to-submit-lab

## What is parallel computing?

Parallel Computing is a way of computing using parallel threads on different physical cores or specialized acceleration e.g. GPUs to parallelize and speed up computing process. 